# Mobile API documentation

The mobile application is now parent-only.

The authoritative contract has moved to [`api-docs/mobile-api.md`](../api-docs/mobile-api.md). This compatibility file is retained so existing internal links do not break.
