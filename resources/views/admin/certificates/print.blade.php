<!doctype html><html><head><meta charset="utf-8"><style>@page{margin:12mm}body{font-family:DejaVu Sans,sans-serif;color:#18202d}.frame{height:175mm;border:3px double #f97316;padding:14mm;text-align:center;box-sizing:border-box}.logo{height:22mm}.school{font-size:22px;font-weight:bold}.type{font-size:30px;text-transform:uppercase;margin:15mm 0 8mm;color:#c2500b}.body{font-size:17px;line-height:2}.name{font-size:27px;font-weight:bold}.meta{margin-top:8mm;font-size:13px}.qr{width:26mm;position:absolute;right:28mm;bottom:25mm}.signature{position:absolute;left:28mm;bottom:28mm;width:55mm;border-top:1px solid #555;padding-top:3mm}</style></head><body><div class="frame">@if($schoolLogo)<img class="logo" src="{{ $schoolLogo }}">@endif<div class="school">{{ $school->trading_name }}</div><div class="type">{{ $certificate->type->label() }}</div><div class="body">Nous attestons que <div class="name">{{ $certificate->student_name }}</div>@if($certificate->formation_name)a suivi la formation <strong>{{ $certificate->formation_name }}</strong>@if($certificate->level), niveau {{ $certificate->level }}@endif@if($certificate->duration_hours), d’une durée de {{ $certificate->duration_hours }} heures@endif@if($certificate->formation_start || $certificate->formation_end),<br>@if($certificate->formation_start)du {{ $certificate->formation_start->format('d/m/Y') }}@endif @if($certificate->formation_end)au {{ $certificate->formation_end->format('d/m/Y') }}@endif@endif.@else reçoit le présent document.@endif @if($certificate->attendance_rate!==null)<br>Taux de présence : {{ $certificate->attendance_rate }} %.@endif @if($certificate->result)<br>Résultat : {{ $certificate->result }}.@endif</div><div class="meta">Nº {{ $certificate->certificate_number }} · Délivré le {{ $certificate->issue_date->format('d/m/Y') }}</div><div class="signature">{{ $certificate->signature_name ?: 'Signature et cachet' }}</div><img class="qr" src="{{ $qrCode }}"></div></body></html>
