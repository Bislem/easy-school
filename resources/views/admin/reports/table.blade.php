<!doctype html><html><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans,sans-serif;font-size:9px}h1{font-size:18px}table{width:100%;border-collapse:collapse}th,td{border:1px solid #bbb;padding:5px;text-align:left}th{background:#eee}</style></head><body><h1>{{ $title }}</h1><p>Période : {{ $from ?: 'début' }} — {{ $to ?: 'aujourd’hui' }}</p>@if($rows->isNotEmpty())<table><thead><tr>@foreach(array_keys($rows->first()) as $column)<th>{{ $column }}</th>@endforeach</tr></thead><tbody>@foreach($rows as $row)<tr>@foreach($row as $value)<td>{{ is_scalar($value)?$value:json_encode($value) }}</td>@endforeach</tr>@endforeach</tbody></table>@else<p>Aucune donnée.</p>@endif</body></html>
